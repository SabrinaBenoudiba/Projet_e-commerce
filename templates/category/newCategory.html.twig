{% extends 'base.html.twig' %}

{% block title %}Nouvelles Categories{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
        <div class="form">
            <h2>Ajouter une catégorie</h2>
            {{ form_start(form) }}
                {{ form_row(form.name, {'attr' :{'class':'form form-control' }}) }}

                <input type="submit" value='sauvegarder' class='btn btn-primary mt-4'>
            {{ form_end(form) }}
        </div>
         <div>
            <a href="{{ path('app_category') }}" class="btn btn-primary">Retour à Nos Catégories</a>
        </div>  
    </div>

{% endblock %}
