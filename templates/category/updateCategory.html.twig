{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            {#Affichage d'un message flash#}
                {% for message in app.flashes('notice')%}
                    <div class="alert alert-success">
                        {{message}}
                    </div>
                {% endfor %}


            {#Affichage du formulaire#}
                <h1 class="mt-5 mb-5">Modification de l'article</h1>
                <div class="col-d-6 offset-md-3">
                    {{ form_start(form) }}
                        <div class="form-group">
                            {{ form_label(form.name) }}
                            {{ form_widget(form.name, { 'attr': {'class':'form-control'}})}}
                        </div>
                        <div class="form-group">
                            <button class="btn btn-primary mt-2 text-white">Mettre à jour</button>
                        </div>
                    {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}